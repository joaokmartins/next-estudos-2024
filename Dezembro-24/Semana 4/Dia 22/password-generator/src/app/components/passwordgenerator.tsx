"use client";

import {useState} from 'react';
import styles from '';

interface = {
    pass:string;
};

export default function passwordgenerator() {
  const [item, setItem] = useState<interface> ({''};
  
  const generator = {
    const password = if(item.pass.length > 8 && < 32) {
      
    }
  }

  const handleSubmitButton = alert(``);

  )

  

    return(

      <div className='styles'>
        <button className='styles'>

        </button>

      </div>

    )
};